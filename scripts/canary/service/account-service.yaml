kind: Service
apiVersion: v1
metadata:
  name: account-service
  labels:
    role: ${TARGET_ROLE}
spec:
  selector:
    app: account-service
    role: ${TARGET_ROLE}
  ports:
  - port: 80
    targetPort: 80
  